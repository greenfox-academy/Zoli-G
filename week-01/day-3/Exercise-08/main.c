#include <stdio.h>

int main() {
	int h1 = 350;
	int h2 = 200;

	// tell if the double of h2 is bigger than h1
	if (h2 * 2 > h1) {
        printf("h1 = %d, h2= %d\n", h1, h2);
        printf("Yaaay! The double of h2 is bigger than h1!");
	}

	return 0;
}
